rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /personas/{docId} {
      allow read, write: if request.auth != null
                         && request.auth.token.email == 'macisenior@gmail.com';
    }
  }
}